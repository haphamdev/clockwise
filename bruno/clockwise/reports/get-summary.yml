info:
  name: get-summary
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/v1/reports/summary
  auth: bearer
  params:
    - name: dateFrom
      value: "2026-03-01"
      enabled: true
    - name: dateTo
      value: "2026-04-04"
      enabled: true
    - name: teamIds
      value: "{{teamId}}"
      enabled: false
    - name: userIds
      value: "{{userId}}"
      enabled: false
    - name: projectIds
      value: "{{projectId}}"
      enabled: false

auth:
  bearer:
    token: "{{accessToken}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.totalHours
      operator: isNumber
    - expression: res.body.totalEntries
      operator: isNumber

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
