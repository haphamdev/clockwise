# CLAUDE.md

Repository guidance now lives in **`AGENTS.md`** (root), which auto-imports the
modular rules under **`.omp/rules/`**:

- `.omp/rules/coding-conventions.md` — code style & implementation patterns
- `.omp/rules/testing.md` — testing (Jest, Vitest, Testcontainers, TDD)
- `.omp/rules/security.md` — authorization, secrets, data integrity, audit

This file is kept only as a pointer to avoid stale, conflicting guidance. Read
`AGENTS.md` and the `.omp/rules/` files instead.
